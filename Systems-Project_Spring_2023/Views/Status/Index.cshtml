@model IEnumerable<Systems_Project_Spring_2023.Models.Status>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Status_desc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Status_desc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Status_code">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Status_code">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Status_code">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
